import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        flex: 1,
        marginTop: 30
    },
    labelStyle: {
        textAlign: 'center',
        fontSize: 35
    },
    inputStyle: {
        textAlign: 'center',
        fontSize: 25
    },
    subLabel: {
        textAlign: 'center',
        color: 'gray'
    },
    resendText: {
        textAlign: 'center',
        color: '#33D65B',
        fontSize: 15
    }
});

export default styles;
