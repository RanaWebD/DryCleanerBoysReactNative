import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        flex: 1,
        marginTop: 30
    },
    labelStyle: {
        textAlign: 'center',
        fontSize: 20
    },
    inputStyle: {
        textAlign: 'center',
        fontSize: 20
    }
});

export default styles;
